const questionsIndex = require("./questions/index");

/**
 * Audit questions in French, categorized by sector.
 * Export the object containing all sector keys (healthcare, technology, etc.)
 */
module.exports = questionsIndex;
